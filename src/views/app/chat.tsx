import { useGetUserProfile } from 'hooks';
import React from 'react'
import { useNavigate } from 'react-router-dom';

const Chat = () => {

  return (
    <div>

    </div>
  )
}

export default Chat;